export enum SeatType {
  REGULAR = "REGULAR",
  PREMIUM = "PREMIUM",
  RECLINER = "RECLINER"
}

export enum SeatStatus {
  AVAILABLE = "AVAILABLE",
  SELECTED = "SELECTED",
  BOOKED = "BOOKED"
}

export enum CensorRating {
  U = "U",
  UA = "UA",
  A = "A",
  S = "S"
}